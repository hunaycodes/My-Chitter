{
  "name": "chitter-challenge-backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "test": "NODE_ENV=test mocha --timeout 10000 --exit",
    "start": "NODE_ENV=development nodemon server",
    "start-prod": "NODE_ENV=production nodemon src/server",
    "test-win": "SET NODE_ENV=test&&mocha --timeout 500000 --exit",
    "start-win": "SET NODE_ENV=development&&nodemon src/server",
    "start-prod-win": "SET NODE_ENV=production&&nodemon src/server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.7",
    "chai-http": "^4.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^6.15.0",
    "mocha": "^10.2.0",
    "mongoose": "^7.0.3",
    "nodemon": "^2.0.21"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}